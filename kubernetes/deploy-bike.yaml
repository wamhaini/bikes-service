apiVersion: v1                    # API version
kind: Service                     # Type of kubernetes resource
metadata:
  name: bike-mysql   # Name of the service
  labels:                         # Labels that will be applied to the service
    app: bike
spec:
  ports:
    - port: 3306
  selector:                       # Selects any Pod with labels `app=book-info-service,tier=mysql`
    app: bike
    tier: mysql
  clusterIP: None
---
apiVersion: apps/v1
kind: Deployment                    # Type of the kubernetes resource
metadata:
  name: bike-mysql-deployment           # Name of the deployment
  labels:                           # Labels applied to this deployment
    app: bike
spec:
  selector:
    matchLabels:                    # This deployment applies to the Pods matching the specified labels
      app: bike
      tier: mysql
  strategy:
    type: Recreate
  template:                         # Template for the Pods in this deployment
    metadata:
      labels:                       # Labels to be applied to the Pods in this deployment
        app: bike
        tier: mysql
    spec:                           # The spec for the containers that will be run inside the Pods in this deployment
      containers:
        - image: mysql                # The container image
          name: mysql
          env:                        # Environment variables passed to the container
            - name: MYSQL_ROOT_PASSWORD
              value: "abc123"           # Read environment variables from kubernetes secrets
          ports:
            - containerPort: 3306       # The port that the container exposes
              name: mysql
---
apiVersion: apps/v1           # API version
kind: Deployment              # Type of kubernetes resource
metadata:
  name: bike-server-deployment    # Name of the deployment
  labels:                     # Labels that will be applied to this deployment
    app: bike-server
spec:
  replicas: 1                 # No. of replicas/pods to run in this deployment
  selector:
    matchLabels:              # The deployment applies to any pods mayching the specified labels
      app: bike-server
  template:                   # Template for creating the pods in this deployment
    metadata:
      labels:                 # Labels that will be applied to each Pod in this deployment
        app: bike-server
    spec:                     # Spec for the containers that will be run in the Pods
      containers:
        - name: bike-server
          image: wamhaini/bike
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8054 # The port that the container exposes
          resources:
            limits:
              cpu: 0.2
              memory: "200Mi"
          env:                    # Environment variables supplied to the Pod
            - name: MYSQL_DB_USERNAME
              value: "root"
            - name: MYSQL_DB_PASSWORD
              value: "abc123"
            - name: MYSQL_DB_HOST
              value: "bike-mysql"
---
apiVersion: v1                      # API version
kind: Service                       # Type of the kubernetes resource
metadata:
  name: bike-server    # Name of the service
  labels:                           # Labels that will be applied to this service
    app: bike-server
spec:
  type: NodePort                    # The service will be exposed by opening a Port on each node and proxying it. Allowing connections from outside the cluster
  selector:
    app: bike-server   # The service exposes Pods with label `app=book-info-service-server`
  ports:                            # Forward incoming connections on port 8080 to the target port 8051
    - name: http
      port: 8054
      targetPort: 8054